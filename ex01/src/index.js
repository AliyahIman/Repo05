function main(){
    // Only change code below this line
    var myFirstString= "I tink therefore I \"am\" -- sentient.",
        mySecondString="No, you only \"think\", so you aren’t."
    // Only change code above this line

    return{
        myFirstString,
        mySecondString
    };
}

console.log(main());
module.exports = main;
